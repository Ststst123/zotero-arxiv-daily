name: Send emails daily
on:
  workflow_dispatch:
  schedule:
    - cron: '0 22 * * *'

jobs:
  calculate-and-send:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          repository: ${{ vars.REPOSITORY }}
          ref: ${{ vars.REF }}

      - name: Setup uv
        uses: astral-sh/setup-uv@v7

      - name: Run script
        env:
          ZOTERO_ID: ${{ secrets.ZOTERO_ID }}
          ZOTERO_KEY: ${{ secrets.ZOTERO_KEY }}
          SENDER: ${{ secrets.SENDER }}
          RECEIVER: ${{ secrets.RECEIVER }}
          SENDER_PASSWORD: ${{ secrets.SENDER_PASSWORD }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          OPENAI_API_BASE: ${{ secrets.OPENAI_API_BASE }}
          CUSTOM_CONFIG: ${{ vars.CUSTOM_CONFIG }}
        run: |
          if [ -n "$CUSTOM_CONFIG" ]; then
            CUSTOM_CONFIG_CONTENT="$CUSTOM_CONFIG" python - <<'PY' > config/custom.yaml
import os
import re

content = os.environ["CUSTOM_CONFIG_CONTENT"]
pattern = re.compile(r"\$\{oc\.env:([^}]+)\}")

def replace_if_invalid_env_name(match):
    env_expr = match.group(1).strip()
    env_name, has_default, default_value = env_expr.partition(",")
    env_name = env_name.strip()
    if re.fullmatch(r"[A-Za-z_][A-Za-z0-9_]*", env_name):
        return match.group(0)
    if has_default:
        return default_value.strip()
    return env_name

print(pattern.sub(replace_if_invalid_env_name, content), end="")
PY
          fi
          [ -f config/custom.yaml ] || touch config/custom.yaml
          echo "Use custom config: "
          cat config/custom.yaml
          uv run src/zotero_arxiv_daily/main.py
